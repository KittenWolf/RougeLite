class Enemy extends Character {
    constructor(health, attack, tile, name) {
        super(health, attack, tile, name);
    }
}

class Rouge extends Enemy {
    constructor(health, attack, tile, name) {
        super(health, attack, tile, name);
    }
}